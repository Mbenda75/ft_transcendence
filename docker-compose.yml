version: '3.9'
 
services:

  db:
    image: postgres
    restart: always
    #environment:
      #POSTGRES_PASSWORD: 0000

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: pma
    links:
      - db
    #environment:
     # PMA_HOST: db
      #PMA_PORT: 3306
      #PMA_ARBITRARY: 1
    restart: always
    ports:
      - 8080:8080
